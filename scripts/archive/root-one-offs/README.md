# Root one-off scripts (archived)

These `fix_*.py` and `patch_*.py` files lived at the repository root as ad-hoc repair scripts for past missions. They were moved here to keep the repo root small; paths inside them may still reference the repo root—run from the repository root or adjust `Path`/`os.chdir` if you reuse one.
