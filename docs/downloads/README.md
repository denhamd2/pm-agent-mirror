# Downloaded decks (local only)

PowerPoint files in this folder are **not tracked in Git** (see root `.gitignore`: `docs/downloads/*.pptx`). Generate decks locally via the Slide Deck MCP / `python3 scripts/generate_presentation.py` and store outputs here or in `~/Downloads/` without committing passcodes or large binaries.
