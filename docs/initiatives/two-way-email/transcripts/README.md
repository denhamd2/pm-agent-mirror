# Refinement transcripts

Place refinement meeting transcripts here as `.md`, `.txt`, or **session PDF exports** (one file per session is fine).

After adding a file, add a bullet link to it in [../CONTEXT.md](../CONTEXT.md) under **Refinement transcripts and notes** so agents reliably discover it.
