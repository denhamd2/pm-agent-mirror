# Archived Cursor commands

Files here are **removed from the Commands palette** (Cursor loads `.md` commands from the parent [`commands/`](../) folder only).

- **`prd-draft.md`** — Legacy conversational PRD interview prompt; use **`/write-prd`** ([`write-prd/SKILL.md`](../../skills/write-prd/SKILL.md)) for the maintained path. Restore by moving the file up one level to `.cursor/commands/` if needed.
