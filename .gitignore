# Node modules
node_modules/
design/node_modules/

# Build outputs
dist/
build/
# Log files (includes ad-hoc Cursor / extension traces under .cursor/*.log)
*.log

# Environment files
.env
.env.local

# Playwright auth / storage state (contains live SUV session cookies - NEVER commit)
.playwright/

# Playwright MCP server logs (may contain URLs with session-sensitive query params; local only)
.playwright-mcp/

# Playwright test artifacts (design/)
design/playwright-report/
design/test-results/

# Local-only auth / credential notes (never commit tokens, PATs, etc.)
docs/confluence-auth-resolved.md
docs/*auth*.md
# Ephemeral triage fallback output (regenerated by customer-issue-triage skill when Confluence unavailable)
docs/customer-issue-triage-output.md

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Cursor MCP project config (secrets); keep ~/.cursor/mcp.json or local copy only
.cursor/mcp.json
!.cursor/mcp.json.example

# Python bytecode (never commit)
**/__pycache__/
*.pyc

# Temporary / local-only scratch (never commit personal dumps)
scratchpad.md
tmp_prd_invocation.txt
!scratchpad.example.md

# Temporary files
*.swp
*~

# MCP cache (optional - keep local only)
# Uncomment if you don't want to track MCP files
# /Users/david.denham/.cursor/projects/

# Slide deck outputs (optional - these are generated)
*.pptx
# Keep large PMF decks local only (not in Git); see docs/downloads/README.md
docs/downloads/*.pptx

# Local development
design/dist/
design/.vite/

# Dashboard build outputs (no longer published to GHE Pages; localhost-only via `cd design && npm run dev`)
dashboard-dist/
assets/

# Public dashboard build output (catalogue + public prototype allowlist published to GHE Pages
# by .github/workflows/deploy-dashboard-public.yml; built into gh-pages branch
# at deploy time, never tracked on main). Build is gated by
# design/vite-plugin-forbidden-imports.ts which blocks Pharos-derived data.
design/dashboard-public-dist/

# Large data files (exceed GitHub 100 MB limit; keep local only)
design/data-bp-durations.ts

# 106/107 scratch dumps (regenerated each run; optional to track)
research/**/brainstorm-analysis/_scratch*.md
research/**/win-loss-analysis/_scratch*.md

# SSA demo video analysis scratch (raw .mov sources + extracted keyframe PNGs;
# the synthesized best-practices markdown in design/references/ IS committed).
design/references/ssa-create-req-videos/source/
design/references/ssa-create-req-videos/frames-location-move/
design/references/ssa-create-req-videos/frames-overlap/

# Talent Acquisition keynote demo scratch (raw .mp4 + extracted keyframe PNGs;
# the synthesized best-practices markdown in design/references/ IS committed).
design/references/talent-acq-videos/source/
design/references/talent-acq-videos/frames/
design/references/talent-acq-videos/frames-*/

