{
  "name": "product-manager-agent",
  "version": "1.0.0",
  "description": "Product Manager Agent Workspace",
  "scripts": {
    "dev:design": "npm run dev --prefix design",
    "cleanup:artifacts": "python3 scripts/cleanup-old-artifacts.py --keep 3",
    "cleanup:artifacts:dry-run": "python3 scripts/cleanup-old-artifacts.py --keep 3 --dry-run"
  }
}
